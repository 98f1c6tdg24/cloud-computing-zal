---
# ZADANIE STUDENCKIE
#
# Uzupełnij playbook tak, aby skonfigurował Apache na maszynie utworzonej przez Terraform.
#
# Wymagania:
# 1. Zaktualizuj cache apt.
# 2. Zainstaluj apache2.
# 3. Uruchom usługę apache2.
# 4. Włącz start apache2 razem z systemem.
# 5. Skopiuj plik files/index.html na /var/www/html/index.html.
#
# Podpowiedzi:
# - użyj modułów: apt, service, copy
# - ustaw become: true
# - po wykonaniu playbooka sprawdź stronę: http://PUBLIC_IP

- name: Zadanie studenckie - konfiguracja Apache
  hosts: webservers
  become: true

  tasks:
    - name: TODO - aktualizacja cache apt
      debug:
        msg: "Zastąp ten task modułem apt."

    - name: TODO - instalacja Apache
      debug:
        msg: "Zastąp ten task instalacją apache2."

    - name: TODO - uruchomienie Apache
      debug:
        msg: "Zastąp ten task uruchomieniem i włączeniem usługi apache2."

    - name: TODO - podmiana pliku index.html
      debug:
        msg: "Zastąp ten task modułem copy."
